const config = {
  BASE_URL: import.meta.env.VITE_GATEWAY_URL,
  APP_KEY: import.meta.env.VITE_APP_KEY,
  ENV: import.meta.env.VITE_ENV,
  appUrl:'https://app.phoenixwallet.io'
};

export default config;
